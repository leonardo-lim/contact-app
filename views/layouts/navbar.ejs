<nav class="navbar navbar-expand-lg navbar-dark bg-dark position-absolute w-100">
  <div class="container">
    <a class="navbar-brand" href="/">Express Contact App</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link <% if (title === 'Homepage') { %> active <% } %>" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <% if (title === 'About') { %> active <% } %>" href="/about">About</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <% if (title === 'Contact' || title === 'Contact Detail' || title === 'Add Contact' || title === 'Edit Contact') { %> active <% } %>" href="/contact">Contact</a>
        </li>
      </ul>
    </div>
  </div>
</nav>